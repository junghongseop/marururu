import { color } from '@maru/design-token';
import { Text } from '@maru/ui';

const StudentItem = () => {
  return (
    <Text fontType="p3" color={color.gray900}>
      여기는 마감된 학생 대상 입학설명회가 있을 구역 입니다.
    </Text>
  );
};

export default StudentItem;
